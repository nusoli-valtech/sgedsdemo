---
phase: 01-setup-foundation
plan: 02
type: execute
wave: 1
depends_on: []
files_modified:
  - scripts/config.js
autonomous: true
requirements:
  - SET-02
tags:
  - eds
  - config
  - identifier-coupling
  - foundation

must_haves:
  truths:
    - "A new file `scripts/config.js` exists at the repo root and exports five named constants — `AEM_AUTHOR_HOST`, `PROJECT_NAME`, `AEM_INSTANCE_ID`, `DAM_PREFIX`, `CONTENT_PREFIX`."
    - "Phase 2 (CFO migration) can `import { AEM_AUTHOR_HOST } from '../../scripts/config.js'` and receive the canonical Author host value."
    - "ESLint passes against the new file (airbnb-base + import/extensions: js: always)."
    - "No existing block or script is modified — Phase 1 establishes the module; Phase 2 deletes the publish-host literals it replaces."
  artifacts:
    - path: "scripts/config.js"
      provides: "Central configuration constants for AEM Author host + project codename"
      exports: ["AEM_AUTHOR_HOST", "PROJECT_NAME", "AEM_INSTANCE_ID", "DAM_PREFIX", "CONTENT_PREFIX"]
      min_lines: 10
  key_links:
    - from: "scripts/config.js"
      to: "fstab.yaml mountpoint host"
      via: "AEM_AUTHOR_HOST literal value"
      pattern: "AEM_AUTHOR_HOST = 'https://author-p23458-e585661\\.adobeaemcloud\\.com'"
    - from: "scripts/config.js"
      to: "paths.json content prefix"
      via: "CONTENT_PREFIX literal value"
      pattern: "CONTENT_PREFIX = '/content/sgedsdemo/'"
---

<objective>
Create `scripts/config.js` — a single new vanilla ESM file with five named-export constants. This is the central source of truth for AEM Author host, project codename, instance ID, and DAM/content path prefixes. Phase 2-5 import from here instead of hardcoding.

Purpose: Closes CF-EXISTING-4 (project-name coupling). Once the module exists, Phase 2 can delete the publish-host literals in `blocks/article-hero/article-hero.js` and `blocks/article-teaser/article-teaser.js` as part of the CFO migration without scope creep.

Output: One new file. Zero existing files touched. Lint-passing. Imports work from any block at depth `blocks/<name>/<name>.js`.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/01-setup-foundation/01-CONTEXT.md
@.planning/codebase/CONCERNS.md
@.planning/research/PITFALLS.md
@./CLAUDE.md

@fstab.yaml
@paths.json
@.eslintrc.js

<interfaces>
<!-- Source-of-truth values derived from existing config files. -->

From `fstab.yaml` mountpoint:
```
mountpoints:
  /: https://author-p23458-e585661.adobeaemcloud.com/bin/franklin.delivery/nusoli-valtech/sgedsdemo/main
```
- AEM_AUTHOR_HOST = `https://author-p23458-e585661.adobeaemcloud.com` (origin only — no path)
- AEM_INSTANCE_ID = `p23458-e585661` (the `<programId>-<envId>` portion of the host)
- PROJECT_NAME = `sgedsdemo` (the project segment in the franklin.delivery path)

From `paths.json`:
```json
{ "mappings": [ "/content/sgedsdemo/:/", "/content/dam/sgedsdemo/:/" ] }
```
- CONTENT_PREFIX = `/content/sgedsdemo/`
- DAM_PREFIX = `/content/dam/sgedsdemo/`

ESLint constraints (`.eslintrc.js`):
- `airbnb-base` extends → single quotes, trailing commas in multiline, semicolons required
- `import/extensions: js: always` → all relative imports must end in `.js` (this file has none, so N/A)
- `linebreak-style: unix` → LF line endings only

Module convention (project): one `export const NAME = value;` per line. No default export. No JSDoc required for static constants but optional file-level header comment is allowed (see `scripts/aem.js:1-11` for the boilerplate banner pattern — do NOT replicate the Adobe banner; this file is project-owned).
</interfaces>
</context>

<tasks>

<task type="auto" tdd="false">
  <name>Task 1: Create scripts/config.js with five named-export constants</name>
  <files>scripts/config.js</files>
  <read_first>
    - fstab.yaml (confirm host + project segment values)
    - paths.json (confirm content/dam prefix values)
    - .eslintrc.js (airbnb-base rule set)
    - .planning/phases/01-setup-foundation/01-CONTEXT.md §`<decisions>` D-01, D-02, D-03 (named exports, minimum-viable key set, no Phase 2 refactor here)
    - scripts/aem.js (read first 20 lines only for module style reference; do NOT import from it)
  </read_first>
  <action>
    Create the file `scripts/config.js` at the repo root (sibling of `scripts/aem.js`, `scripts/scripts.js`, `scripts/editor-support.js`). Contents must be EXACTLY (preserving blank lines, two-space indent N/A here, single quotes, semicolons, LF line endings):

    ```
    /**
     * Central project configuration.
     *
     * Per D-02 (.planning/phases/01-setup-foundation/01-CONTEXT.md): minimum-viable key set.
     * Add more identifiers as Phase 2-5 require them; do not pre-design future surfaces here.
     *
     * Source of truth:
     * - AEM_AUTHOR_HOST / AEM_INSTANCE_ID / PROJECT_NAME — derived from `fstab.yaml` mountpoint.
     * - CONTENT_PREFIX / DAM_PREFIX                     — derived from `paths.json` mappings.
     */

    export const AEM_AUTHOR_HOST = 'https://author-p23458-e585661.adobeaemcloud.com';
    export const PROJECT_NAME = 'sgedsdemo';
    export const AEM_INSTANCE_ID = 'p23458-e585661';
    export const DAM_PREFIX = '/content/dam/sgedsdemo/';
    export const CONTENT_PREFIX = '/content/sgedsdemo/';
    ```

    Constraints:
    - Use single quotes for all string literals (airbnb-base).
    - End every export with a semicolon.
    - LF line endings only (Unix).
    - Each `export const` on its own line — no batched `export const A = ..., B = ...`.
    - The file MUST be importable as `import { AEM_AUTHOR_HOST } from '../../scripts/config.js'` from a block at `blocks/<name>/<name>.js`. Vanilla ESM, no bundler — the `.js` extension is mandatory per `.eslintrc.js:18`.
    - Do NOT add a default export.
    - Do NOT import from `scripts/aem.js` or any other file — this is a leaf module.
    - Do NOT modify any other file. Do NOT add to `package.json`. Do NOT create a `tsconfig.json` or types file.

    No call sites are updated in this plan. Per D-03, Phase 2 deletes the publish-host literals in `blocks/article-hero/` and `blocks/article-teaser/` as part of the CFO migration. Phase 1 only stands up the module.
  </action>
  <acceptance_criteria>
    - `test -f scripts/config.js` returns true (file exists at the exact path).
    - `grep -c "^export const " scripts/config.js` returns exactly `5` (the five required exports, each on its own line).
    - `grep -F "AEM_AUTHOR_HOST = 'https://author-p23458-e585661.adobeaemcloud.com';" scripts/config.js` matches.
    - `grep -F "PROJECT_NAME = 'sgedsdemo';" scripts/config.js` matches.
    - `grep -F "AEM_INSTANCE_ID = 'p23458-e585661';" scripts/config.js` matches.
    - `grep -F "DAM_PREFIX = '/content/dam/sgedsdemo/';" scripts/config.js` matches.
    - `grep -F "CONTENT_PREFIX = '/content/sgedsdemo/';" scripts/config.js` matches.
    - `grep -c "export default" scripts/config.js` returns `0` (no default export per D-01).
    - `grep -c "^import " scripts/config.js` returns `0` (leaf module — no imports).
    - `npm run lint:js -- scripts/config.js` exits 0.
    - File contains a JSDoc-style header comment block referencing D-02 and the source-of-truth files (fstab.yaml, paths.json).
    - File line endings are LF only: `file scripts/config.js` does NOT report `CRLF line terminators`.
  </acceptance_criteria>
  <verify>
    <automated>test -f scripts/config.js && [ "$(grep -c '^export const ' scripts/config.js)" = "5" ] && grep -qF "AEM_AUTHOR_HOST = 'https://author-p23458-e585661.adobeaemcloud.com';" scripts/config.js && grep -qF "PROJECT_NAME = 'sgedsdemo';" scripts/config.js && grep -qF "AEM_INSTANCE_ID = 'p23458-e585661';" scripts/config.js && grep -qF "DAM_PREFIX = '/content/dam/sgedsdemo/';" scripts/config.js && grep -qF "CONTENT_PREFIX = '/content/sgedsdemo/';" scripts/config.js && [ "$(grep -c 'export default' scripts/config.js)" = "0" ] && [ "$(grep -c '^import ' scripts/config.js)" = "0" ] && npm run lint:js -- scripts/config.js</automated>
  </verify>
  <done>scripts/config.js exists with five named exports at the correct values; no default export; no imports; lint clean.</done>
</task>

<task type="auto" tdd="false">
  <name>Task 2: Smoke-import the new module from a temporary script to confirm ESM import works</name>
  <files>(none committed — temporary verification only)</files>
  <read_first>
    - scripts/config.js (the file just created)
    - .planning/phases/01-setup-foundation/01-CONTEXT.md §`<code_context>` "Vanilla ESM, no bundler"
  </read_first>
  <action>
    Create a TEMPORARY one-shot smoke test to prove the module is importable as native ESM. Do NOT commit this file.

    Step 1: Create `/tmp/sgedsdemo-config-smoke.mjs` with contents:
    ```
    import { AEM_AUTHOR_HOST, PROJECT_NAME, AEM_INSTANCE_ID, DAM_PREFIX, CONTENT_PREFIX } from './scripts/config.js';
    const ok = AEM_AUTHOR_HOST === 'https://author-p23458-e585661.adobeaemcloud.com'
      && PROJECT_NAME === 'sgedsdemo'
      && AEM_INSTANCE_ID === 'p23458-e585661'
      && DAM_PREFIX === '/content/dam/sgedsdemo/'
      && CONTENT_PREFIX === '/content/sgedsdemo/';
    if (!ok) { console.error('config.js export values mismatch'); process.exit(1); }
    console.log('config.js smoke OK');
    ```

    Step 2: Run from the repo root: `node --input-type=module -e "$(cat /tmp/sgedsdemo-config-smoke.mjs)"`

    Step 3: Confirm output is `config.js smoke OK` and exit code is 0.

    Step 4: Delete `/tmp/sgedsdemo-config-smoke.mjs`.

    Step 5: Confirm `git status` does NOT list `/tmp/sgedsdemo-config-smoke.mjs` (it's outside the repo) and only shows `scripts/config.js` as new.

    Note: this validates the module syntactically loads. Browser-context use (the actual Phase 2-5 consumer) is identical because the file is plain ES module syntax with no Node-specific APIs.
  </action>
  <acceptance_criteria>
    - Smoke command output contains exactly `config.js smoke OK` on stdout.
    - Smoke command exit code is 0.
    - `/tmp/sgedsdemo-config-smoke.mjs` does NOT exist after the task (cleanup confirmed).
    - `git status --porcelain | grep -v "^??.*\\.planning/"` lists `scripts/config.js` as the only new file under tracked paths (no other repo files modified by this task).
  </acceptance_criteria>
  <verify>
    <automated>node --input-type=module -e "import { AEM_AUTHOR_HOST, PROJECT_NAME, AEM_INSTANCE_ID, DAM_PREFIX, CONTENT_PREFIX } from './scripts/config.js'; const ok = AEM_AUTHOR_HOST === 'https://author-p23458-e585661.adobeaemcloud.com' && PROJECT_NAME === 'sgedsdemo' && AEM_INSTANCE_ID === 'p23458-e585661' && DAM_PREFIX === '/content/dam/sgedsdemo/' && CONTENT_PREFIX === '/content/sgedsdemo/'; if (!ok) { console.error('mismatch'); process.exit(1); } console.log('config.js smoke OK');"</automated>
  </verify>
  <done>Module loads via Node ESM, all five exports return expected values, no temp files left behind.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Build/runtime → AEM endpoints | Static identifiers (host, project name) consumed by future fetch calls; values come from authored config files (`fstab.yaml`, `paths.json`) checked into the repo |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-01-02-01 | Information Disclosure | scripts/config.js — AEM_AUTHOR_HOST exposed to browser | accept | The Author host is already public information (it appears in `fstab.yaml`, which is committed). The mountpoint URL is required for Helix to function and is not a secret. No credentials, tokens, or secrets are exposed by this module. |
| T-01-02-02 | Tampering | scripts/config.js — bad value injected via supply chain | mitigate | Acceptance criteria use `grep -F` exact-string match against the canonical values; any drift fails verification. The five constants are derived from existing source-of-truth files (`fstab.yaml`, `paths.json`) so a malicious change can be cross-checked. |
| T-01-02-03 | Repudiation | n/a | accept | Static constants module; no runtime side effects, no logging, no I/O. n/a — pure data module. |

This plan is configuration-only. The single security-adjacent fact is that `AEM_AUTHOR_HOST` becomes a single grep target — if a future Phase introduces an attacker-controllable substitution path, all consumers are mitigated together. Defense-in-depth via centralization.
</threat_model>

<verification>
- `scripts/config.js` exists with the exact contents prescribed.
- All five named exports resolve to the canonical values.
- ESM `import` works from a Node context (proves browser ESM will too — same syntax).
- `npm run lint` exits 0 (full repo).
- `git status` shows exactly one new file under `scripts/`.
</verification>

<success_criteria>
- New file at `scripts/config.js` with five named-export constants.
- Values match `fstab.yaml` host and `paths.json` mappings exactly.
- File is import-ready for Phase 2 consumers (`import { AEM_AUTHOR_HOST } from '../../scripts/config.js'`).
- Lint passes; no other files modified.
</success_criteria>

<output>
After completion, create `.planning/phases/01-setup-foundation/01-02-SUMMARY.md` capturing:
- The new file path and contents (full file).
- The smoke-import command and its `config.js smoke OK` output.
- `git status --porcelain` showing one untracked file.
- Phase 2 hand-off note: which call sites in `blocks/article-hero/article-hero.js` and `blocks/article-teaser/article-teaser.js` are scheduled for replacement (referenced via D-03), but NOT touched here.
</output>
